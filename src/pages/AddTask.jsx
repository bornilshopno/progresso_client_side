

const AddTask = () => {
    return (
        <div>
            Add Task

                {/* const { title, description, priority, dueDate } = req.body; */}
        </div>
    );
};

export default AddTask;